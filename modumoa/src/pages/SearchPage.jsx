import React from 'react';
import SearchFrame from '../components/Search/SearchFrame';

const Search = () => {
    return (
        <div>
            <SearchFrame/>
        </div>
    );
};

export default Search;